#pragma once

#define ever ;;

constexpr static int PORT_SERVER = 6969;
constexpr static int PORT_CLIENT = 2020;
constexpr static int address_family = 2; // same as = AF_INET, but without including header